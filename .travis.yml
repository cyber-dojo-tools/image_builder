services: docker
language: generic
branches:
  only:
  - master
git:
  depth: 5

script:
    - set -e
    - ./pipe_build_test.sh
    - echo ${DOCKER_PASSWORD} | docker login --username ${DOCKER_USERNAME} --password-stdin
    - ./src/push_tools.sh
